<!-- Sidebar Start -->
<div class="sidebar pb-3 pt-5">
	<nav class="navbar-admin bg-light navbar-light w-100 ps-3">
		<a href="index.html" class="navbar-brand mx-4 mb-3">
			<h3 class="text-primary">USER ADMIN</h3>
		</a>
		<div class="d-flex align-items-center ms-1 mb-4">
			<div class="position-relative">
				<img class="rounded-circle" src="{{ asset('/images/' ~ app.user.photo)}}" alt="" style="width: 40px; height: 40px"/>
				<div class="bg-success rounded-circle border border-2 border-white position-absolute end-0 bottom-0 p-1"></div>
			</div>
			<div class="ms-3">
				<h6 class="mb-0">{{ app.user.Nombre }} {{ app.user.Apellidos }}</h6>
				<span>{{ app.user.roles['0'] }}</span>
			</div>
		</div>
		<div class="navbar-nav w-100">
			<a href="{{ path('app_admin_panel') }}" class="nav-item nav-link active">
				<i class="fa fa-tachometer-alt me-2"></i>Panel Admin</a>

			<a href="#" class="nav-item nav-link">
				<i class="fa fa-user me-2"></i>Perfil</a>
			<a href="{{ path('app_admin_catalogo') }}" class="nav-item nav-link">
				<i class="fa fa-book-open me-2"></i>Catálogo</a>
			<a href="{{ path('app_producto_new') }}" class="nav-item nav-link">
				<i class="fa fa-plus-circle me-2"></i>Crear Producto</a>
			<a href="{{ path('app_admin_editar') }}" class="nav-item nav-link">
				<i class="fa fa-edit me-2"></i>Editar Producto</a>
			<a class="btn btn-primary m-4" href="#">LOG OUT</a>
		</div>
	</nav>
</div>
<!-- Sidebar End -->
