{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
 {# {{ form(registration_form) }} #}
    {{ dump(pedidos) }}
    {{ dump(compras) }}

{% endblock %}
